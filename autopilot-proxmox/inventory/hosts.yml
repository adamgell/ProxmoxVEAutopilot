---
# All Proxmox API calls are made from the controller via the uri module.
# No SSH connections to VMs — everything goes through the Proxmox REST API.
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
