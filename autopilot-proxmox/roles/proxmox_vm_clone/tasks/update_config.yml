---
# Update the cloned VM configuration with APHVTools settings.
# Retries on lock contention (mirrors PowerShell maxConfigAttempts=6, 5s delay).

- name: Build config update body
  ansible.builtin.set_fact:
    _config_body:
      name: "{{ _vm_serial | default(vm_name) | regex_replace('[^A-Za-z0-9\\-]', '') }}"
      cores: "{{ vm_cores }}"
      memory: "{{ vm_memory_mb }}"
      cpu: "host"
      balloon: "0"
      agent: "enabled=1,type=virtio"
      scsi0: "{{ _current_scsi0 | proxmox_disk_serial(_vm_identity.disk_serial) }}"
      net0: "virtio,bridge={{ proxmox_bridge }},firewall=0{{ ',tag=' + (proxmox_vlan_tag | string) if proxmox_vlan_tag else '' }}"
      tags: "autopilot"

- name: Add SMBIOS1 to config
  ansible.builtin.set_fact:
    _config_body: "{{ _config_body | combine({'smbios1': _smbios1_string}) }}"
  when: _smbios1_string is defined and _smbios1_string | default('') | length > 0

- name: Add VirtIO ISO to config
  ansible.builtin.set_fact:
    _config_body: "{{ _config_body | combine({'ide3': proxmox_virtio_iso + ',media=cdrom'}) }}"
  when: proxmox_virtio_iso is defined and proxmox_virtio_iso | default('') | length > 0

# When the web backend compiled the sequence into a per-VM answer ISO,
# the template's baked-in sata0 (pointing at the static autounattend.iso)
# gets replaced with the hash-addressed ISO so this VM picks up its own
# unattend. When _answer_iso_volid is absent the clone keeps the
# template's sata0 untouched — preserves the pre-B.2 code path exactly.
- name: Swap in per-VM answer ISO on sata0
  ansible.builtin.set_fact:
    _config_body: "{{ _config_body | combine({'sata0': _answer_iso_volid + ',media=cdrom'}) }}"
  when: _answer_iso_volid is defined and _answer_iso_volid | default('') | length > 0

- name: Update cloned VM configuration with lock-contention retry
  ansible.builtin.uri:
    url: "{{ proxmox_api_base }}/nodes/{{ proxmox_node }}/qemu/{{ vm_vmid }}/config"
    method: PUT
    body_format: form-urlencoded
    body: "{{ _config_body }}"
    headers:
      Authorization: "{{ proxmox_api_auth_header }}"
    validate_certs: "{{ proxmox_validate_certs }}"
    status_code: [200]
  register: _config_result
  retries: "{{ clone_config_retries | default(6) }}"
  delay: "{{ clone_config_delay | default(5) }}"
  until: _config_result.status == 200

# 'args' is hardcoded to root@pam in PVE::API2::Qemu::check_vm_modify_config_perm
# — literal string match against the authuser, so API tokens (even
# root@pam!<name> with privsep=0) never pass. The only way is a short-lived
# ticket obtained via /access/ticket with password auth. The web handler
# preflights that _proxmox_root_ticket/_proxmox_root_csrf_token are set;
# an auth failure here means the ticket expired mid-run (unlikely in <2h)
# or the password rotated out-of-band.
- name: Apply SMBIOS args via root@pam ticket (required for chassis-type override)
  when: _smbios_args is defined and _smbios_args | length > 0
  block:
    - name: Build merged args string (preserve any existing args on the clone)
      ansible.builtin.set_fact:
        _merged_args: >-
          {{ ((_clone_config.json.data.args | default('')) ~ ' ' ~ _smbios_args) | trim }}

    - name: Update args field via root@pam ticket
      ansible.builtin.uri:
        url: "{{ proxmox_api_base }}/nodes/{{ proxmox_node }}/qemu/{{ vm_vmid }}/config"
        method: PUT
        body_format: form-urlencoded
        body:
          args: "{{ _merged_args }}"
        headers:
          Cookie: "PVEAuthCookie={{ _proxmox_root_ticket }}"
          CSRFPreventionToken: "{{ _proxmox_root_csrf_token }}"
        validate_certs: "{{ proxmox_validate_certs }}"
        status_code: [200]
      register: _args_result
      retries: "{{ clone_config_retries | default(6) }}"
      delay: "{{ clone_config_delay | default(5) }}"
      until: _args_result.status == 200
      no_log: true
