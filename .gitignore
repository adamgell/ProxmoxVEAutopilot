.DS_Store
__pycache__/
*.pyc
.venv/
*.retry
.pytest_cache/

# Secrets — never commit the real vault
autopilot-proxmox/inventory/group_vars/all/vault.yml
# Per-tenant Entra vault files generated by new_entra_app_registration.ps1
# (plaintext secrets; keep out of git, encrypt long-lived copies with ansible-vault)
autopilot-proxmox/inventory/group_vars/all/vault_entra-*.yml
autopilot-proxmox/.env
output/

# docs (allow screenshots for README)
docs/superpowers/

# Session artifacts from Claude Code superpowers tooling
.superpowers/

# Local worktrees for parallel feature work
.worktrees/

# .NET build/signing output
bin/
obj/
autopilot-agent/artifacts/

# Frontend dependency/build output
autopilot-proxmox/frontend/node_modules/
autopilot-proxmox/frontend/dist/
autopilot-proxmox/frontend/test-results/
