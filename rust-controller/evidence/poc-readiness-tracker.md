# Rust controller production-candidate PoC tracker

User requested goal tracking on 2026-09-05. This tracker distinguishes a locally verified production candidate from authorization to deploy or from proven production readiness. The approved replacement design remains authoritative.

## Authority boundary

- Build and verify on this macOS in the isolated worktree with Astra subagents.
- Real Proxmox and production controller `192.168.2.4` are read-only. No queue claims, sweeps, deployments, schema writes, VM operations, agent work, or tenant actions there.
- Disposable non-production mutation proof and production cutover each require separate approval. A goal to become ready does not grant those approvals.
- RustedOutClient is excluded. Reliability, OOBE, remote control, operator UX, adoption and other downstream product work wait for Rust contracts to stabilize.

## Restarted verification checkpoint (2026-09-10)

### Final owned Linux full-run result (2026-09-10)

- The corrected exact-source Linux/amd64 owned-v1 run completed successfully in `restart-task9-owned-full-4`. The bounded Docker invocation exited `0` within its 1,820-second bound; the retained invocation record is `0025.json` and the complete stdout/stderr are `0025.stdout` / `0025.stderr`.
- Every executed Rust target reported `test result: ok`: operation-controller (68 + 73), scheduler transition (57), postgres-store durability (136), registration (59), postgres-store native (64), scheduler/postgres (70), osdeploy-adapter (6 + 20), plus the zero-test/doc-test companions. No `test result: FAILED`, stack overflow, OOM, or fatal runtime marker is present.
- The run used source `49b08779edb1d9d5f3c2b6247cbce77bd6f33751`, runner image `sha256:d2a7622623953ba9342e11ed1d6df00d8dc62c1d62713bdf94e70f9135cb459c`, PostgreSQL image `sha256:57c72fd2a128e416c7fcc499958864df5301e940bca0a56f58fddf30ffc07777`, and launcher `2d977b0b8727ff3aded6277a0ddc44aa0b4ee5d7`. `state.json` records cgroup2, and the numbered inspection records establish the 4 GiB runner / 6 GiB PostgreSQL limits, zero cgroup memory OOM/event counters, and exact session/identity bindings.
- This closes the full Linux execution gate, but does not by itself close restart/recovery, Python/Ansible handoff, rollback/export, operator acceptance, or production cutover gates.

### Guest-action contract refresh (2026-09-10)

- The bounded `GuestActionIdentity` contract is committed through `d9f8949811d600faf308b565f8666213d82609af` (implementation source `da8a40218db163ff367041bb8814b0a33321fee1`). It admits exactly the five guest-action stages, binds `step_id == operation_id`, preserves the original attempt identity, and fixes the initial retry count at zero. Focused and full macOS adapter tests pass; the callback contract gap and intentionally deferred session/result exposure are recorded in `callback-contract-gap.md`.
- A fresh Linux/amd64 image was built from the guest-action source and inspected as `sha256:4dd13f3c600b2da07c580d5a7d0bf2263a0429ee2029c8e35524a394fd9c740b`. The owned-v1 smoke gate passed in `restart-task9-owned-guest-smoke-2/` with source/image/launcher bindings and zero cgroup OOM/event counters. The subsequent exact-source full run is recorded below; this smoke record remains a focused intermediate checkpoint.
- The subsequent exact-source full owned-v1 run completed successfully in `restart-task9-owned-full-guest-1/`. Its retained Cargo output (`0026.stdout`) records 23 successful test-result groups across the operation-controller, scheduler, postgres-store, registration, native, OSDeploy adapter, and documentation companions; the bounded invocation and all independent inspection records exited cleanly. It used source `ac03e96caa70fadd9a572f5c206d03e1ec1e0121`, launcher `e30454e72b132055dcf8aba926a182957ac19f99`, runner `sha256:6c2c32025ce3a2d610c5f3be18d27b1f78a60e6b2e60257023ba8c36503fcdda`, PostgreSQL `sha256:57c72fd2a128e416c7fcc499958864df5301e940bca0a56f58fddf30ffc07777`, and zero cgroup memory OOM/event counters. This supersedes the pre-guest-action full-run as the current Linux execution proof; service-process recovery and the broader readiness gates remain open.

### Durable fixture-log milestone (2026-09-10)

- Test-only `pve-port` support now provides a checksummed, canonical, length-framed attempt ledger with `sync_data()` ordering, fail-closed replay, duplicate-attempt preservation, and no-overwrite creation. Commit `961ef7283cf46ba2789c3ac2b68e8df48e99fe70`; four focused tests and targeted `-D warnings` Clippy pass. This closes only the durable submission-attempt log substrate. Accepted world/effect records, IPC, daemon restart, and independent controller-process recovery remain open.
- The same test-only substrate now persists accepted synthetic VM transitions bound to prior nonduplicate attempts and reconstructs them on replay. Commit `0c157d86caa8074994f11e3837128da3cb9d30dc`; formatting, six focused tests, and targeted strict Clippy pass. Stale before-state, duplicate acceptance, reordered frames, and partial trailing effects fail closed. IPC, daemon restart, task/UPID persistence, and independent controller-process recovery remain open.
- A bounded test-only Unix-socket fixture daemon is now committed at `8e7409633c48e9b5255ecd07d2c6b2aaecfe643b`. It separates controller-facing status/attempt requests from supervisor-only shutdown, bounds request reads, and proves subprocess control isolation plus durable duplicate-ledger behavior. Formatting, six ledger tests, two daemon protocol tests, and targeted strict Clippy pass. Daemon restart/rebind, world/task protocol, controller wiring, and A/B worker recovery remain open.
- The daemon endpoint lifecycle is now covered at `a1bfd719e3462bf409dda73555515faf194ae8fe`: killed-daemon stale endpoints refuse direct restart; supervisor cleanup validates ownership and socket types; verified cleanup permits rebind and ledger recovery; substituted endpoints and corrupt ledgers fail closed. Formatting, four daemon tests, and targeted strict Clippy pass. The daemon still exposes no world/task protocol and is not wired to controller workers.
- The closed client protocol now exposes accepted synthetic effects and per-VM world status at `d0fed1cf530007f35e65432fdd70cd382c615dbd`. Effects require an existing nonduplicate attempt, commit before reply, reject stale before-state/repeated effects, and survive daemon restart; supervisor sockets reject client-only operations. Formatting, six ledger tests, five daemon tests, and targeted strict Clippy pass. Controller wiring, task/UPID state, and A/B worker recovery remain open.
- The first typed Clone contract is feature-gated and committed at `9ecd6c5` (full source `9ecd6c5…`): validated request envelopes preserve the original provisioning binding, and receipts bind fixture identity, request digest, sequence, node/source VM, synthetic `qmclone`, and `fake@pve`. The reusable daemon/ledger support is exported behind the default-disabled `fixture-ipc` feature at `25e4f56`; feature-enabled focused tests pass after the macOS Unix-socket timeout portability fix `4a29b46`. This is still a message/fixture contract, not controller capability or worker recovery.
- The sealed `fixture-ipc::ControllerFixturePort` checkpoint seam is committed at `3f2f4ee`. It extends the existing provisioning capability without exposing production observer mutation, delegates `NativeFakePve` to its current checkpoint, and has a trait-object pause-barrier test plus a negative observer compile-fail contract. Feature docs, default checks, and strict Clippy pass. Dispatch-permit adaptation, typed IPC provisioning, and controller Clone acceptance remain open.
- The consuming dispatch permit now accepts the sealed `ProvisioningFakePort` at `e34eacc`; the follow-up `e8bdd27`/`feaf463` corrected all `Arc<NativeFakePve>` callers to borrow the sealed implementation. All Postgres-store targets compile, 22 doctests pass, and strict Clippy is green. This preserves the existing NativeFakePve path; fixture IPC submission and daemon-backed provisioning facts remain open (controller ownership/helper adaptation is recorded separately at `f78bb5f`).
- The controller capability seam is now wired behind the default-disabled `operation-controller/fixture-ipc` feature at `f78bb5f`: `OsDeployController::new_fixture` accepts the sealed `ControllerFixturePort`, while the default `Arc<NativeFakePve>` constructor and collection/send/admission path remain unchanged. Default and feature all-target checks, five feature library tests, feature doctests, formatting, and strict Clippy pass. This is controller wiring only; the fixture client still lacks daemon-owned provisioning facts and a real IPC Clone round-trip.
- The fixture daemon now exposes a bounded, digest-bound `accepted_effect` recovery read at `1254a94`. It distinguishes no attempt, attempt-only, and durably committed effect; rejects a mismatched original digest; performs no ledger append; and returns the same effect after daemon kill/restart. Feature/default daemon suites, seven durable-log tests, formatting, and strict Clippy pass. This is recovery-read substrate only; daemon-generated task/UPID receipts, `ProvisioningFakePort`, controller Clone acceptance, and worker takeover remain open.
- A feature-gated `FixtureReadClient` is committed at `488cf90`, providing bounded read-only `status`, `world`, and digest-bound `accepted_effect` calls. Connect/write/read share one Tokio deadline; replies are strictly framed and shape-checked; denial, malformed/oversized/truncated frames, stalled peers, and invalid identities are covered. Default and feature all-target tests plus strict Clippy pass, and a daemon test proves reads do not alter ledger bytes. This enables later capability wiring but is not itself `ProvisioningFakePort` or controller acceptance.
- Capability boundaries are pinned at `f0abd54`: feature doctests prove `FixtureReadClient` cannot be passed as `ProvisioningFakePort`, `ControllerFixturePort`, or `PvePreflightReadPort` (22 feature doctests pass). The seam plan now maps all required methods to the daemon facts still absent—identity/configuration, inventory, preflight, media, timestamps, and task/UPID state—and explicitly rejects placeholder success. The next increment is a daemon-owned snapshot/protocol implementation, not an adapter that infers facts from incomplete world state.
- A bounded supervisor-seeded inventory snapshot prefix is committed at `4c7f6c7`. The feature-gated daemon loads a strict, capped `inventory.json` once at startup and exposes explicit unavailable or timestamped node inventory (at most one VM) through `FixtureReadClient::snapshot()`. Missing/corrupt seeds, restart stability, immutable startup observations, and unknown/oversized data are covered; feature/default daemon suites, 26 feature library tests, formatting, and strict Clippy pass. This is historical inventory only and requires accepted-effect/generation reconciliation before preflight use; storage, network, task/UPID, full identity/configuration, mutation capability, controller Clone acceptance, and worker takeover remain open.
- Commit `2f1cfe8` adds a validated typed projection of that historical snapshot, retaining fixture/node identity and the original observation timestamp while rejecting mismatches, malformed VM identities, stale/future facts, and invalid age policies. Focused projection tests, formatting, and strict feature Clippy pass. Missing power/configuration/provisioning/task facts remain explicit `unavailable`; no preflight or dispatch authority is granted.
- Commit `aa2d278` adds identity-bound, feature-gated task observations via supervisor-seeded `task.json` and `FixtureReadClient::task()`. Records bind fixture, node, operation, request digest, and UPID with explicit absent/running/succeeded/failed states; malformed, oversized, mismatched, and invalid records fail closed. Restart, client, daemon, all-target Clippy, and formatting checks pass. This is historical read-only state loaded per daemon lifetime, not task execution or durable transition persistence; controller Clone and worker takeover remain open.
- Commit `76d5e01` adds a separate opt-in `FixtureMutationClient` typed Clone route. A supervisor seed pins the complete validated request and explicit synthetic resulting VM state; the daemon records every attempt before duplicate/binding checks, generates the UPID, and syncs receipt plus effect before replying. Five Clone-contract tests, seven ledger tests, eight daemon tests (one subprocess entry ignored), default suites, formatting, and strict feature Clippy pass. Restart recovers the accepted effect/receipt. This is one exact seeded synthetic mutation, not configuration discovery, full task progression, an `OsDeployController` round-trip, or worker takeover.
- Commit `1a91843` adds an executable Clone-admission prerequisite matrix. Against the real evaluator, independently missing node, storage, bridge, inventory, identity, source/target configuration, source power, media, or complete-coverage facts close admission; absent target power is correctly allowed only when target absence is independently proven. Feature admission tests (3), default evaluator tests (25), formatting, and strict Clippy pass. The accompanying mapping records the exact daemon facts and ordering still required for a PostgreSQL/controller round-trip; no incomplete client was granted provisioning capability.

- Native PostgreSQL restart/recovery cases passed for restart fencing, unknown-state reconciliation, receipt reload, aged-infrastructure continuation, and response-loss reconciliation. Python producer/proof-wait/proof-coordination contracts passed under Python 3.12, and `api-compat` passed 18 runtime plus 2 intended compile-fail documentation tests. Evidence is preserved in `restart-recovery-1/acceptance.md`; this closes contract-level recovery/compatibility only, not independent service-process takeover or legacy-writer quiescence.
- Fresh local all-feature `controller-service` execution passed 58 unit/runtime and 44 service tests, including startup denial, readiness fault latching, observation cancellation, no-writer guarantees and bounded child cleanup. Output is retained as `restart-recovery-1/controller-service.log`; this closes the service observation/health package gate only, not the sixteen-stage OSDeploy workflow or independent process takeover.

- Historical checkpoint: source `8f07247b41e738923ee61e610d4400102b208637` included the durable table expectation correction (`557bd12`), Linux private fixture-family selector (`3362edf`), and Linux-only loopback fixture timeout correction (`8f07247`). The current source and final Linux evidence are recorded above.
- The final macOS four-package regression at `557bd12` passed 665 tests with 3 intentionally ignored; the complete receipt and logs are retained as `restart-task9-macos-full-2.*`.
- An exact-source Linux AMD64 image was rebuilt from `8f07247` (`sha256:82b70282d5c4278b04ed15b67684fdb840235f22b6c27d759047dda59f855e2b`) and passed the repository Compose proof: three competing workers, PostgreSQL fixture, cancellation/recovery, Linux native adapter and descendant cleanup. The bounded run passed 12 lifecycle tests plus 4 native PostgreSQL tests; receipt/logs are `restart-task9-linux-compose-1.*`.
- These historical results established a fresh local Linux artifact and isolated synthetic/native adapter proof. The owned-v1 full Linux gate is now closed by `restart-task9-owned-full-4`; sixteen-stage service execution, Python/Rust single-writer handoff, and production readiness remain open.
- Compatibility-side Python contracts were rerun locally with explicit Python 3.12 under the bounded supervisor: producer contract 2/2, proof-wait 5/5, and proof-coordination 1/1. The earlier system-Python 3.9 selector failure is retained as environment evidence; no production Python code changed.
- The approved owned-v1 launch primitive is committed at `fd18b61`, with nine synthetic contract/supervision tests passing and an independent Astra review. It performs closed resource/receipt admission and bounded smoke/full invocation, but intentionally does not claim periodic resource/catalog observation, binary/export sealing, protected-inventory cleanup, or Linux runtime qualification until those remaining layers are implemented and reviewed.
- First authorized owned-v1 smoke admission was attempted under `ac40427` and failed closed at the required cgroup filesystem-type check: OrbStack's container read returned `UNKNOWN` for `/sys/fs/cgroup` rather than the admitted `cgroup2fs` value. PostgreSQL identity/default settings, 4 GiB tmpfs, 6 GiB memory, no-swap limits and 64 MiB `/dev/shm` were admitted before that point. The exact owned PG container and complete numbered receipts remain preserved; the check was not weakened.
- Follow-up owned-v1 smoke under `9aa3799` passed the approved local profile and exact smoke test. The BusyBox filesystem-name limitation is handled by requiring the `/sys/fs/cgroup` mountinfo record to report `cgroup2`, while retaining private-root, cgroup-cap/event, memory, identity, default-setting and tmpfs checks. Evidence is preserved in `restart-task9-owned-smoke-7/` with receipt `9aa3799`; the launcher intentionally reports `qualification: INCOMPLETE` because full Linux and recovery qualification remain outstanding.
- Owned-v1 `full` mode under `06ed954` reached the Rust runner and completed within the bounded supervisor, but failed with 85 passed / 51 failed in the combined four-package command. Earlier native PostgreSQL and focused service groups remain green; this is an integration qualification failure, not production proof. Evidence and retained resources are in `restart-task9-owned-full-1/`.
- Historical native Linux/arm64 diagnostic image built from exact source `0134a8dcf2f9662198c4abb887104b607088a860` (image `sha256:b230719fccfb02fdef55e37872065d821a8bd44cd491b65cceee2c7e5d6904e9`) reproduced the pre-fix durable-test failures (46 passed / 27 failed). The timestamp repair and final exact-source AMD64 run above supersede that open qualification checkpoint.

## Gates

| Gate | Current evidence / next action |
| --- | --- |
| Durable foundation, scheduler and synthetic adapter | Prior local acceptance retained in foundation evidence; not proof of live replacement |
| Native fake-PVE identity/clone/config/start | Accepted native-controller evidence; no live mutation authority |
| Authenticated cluster observation | Accepted authenticated-observation evidence |
| Selected node/network visibility contracts | Accepted node-network-visibility evidence |
| Selected-node service integration | Accepted for c429807443f80b74530cbd10af334e50e97b695b; see selected-node-service-acceptance.md for exact macOS/Linux execution limits |
| Artifact and OSDeploy input contracts | Artifact identity/byte-match library accepted at 15fadd049ee7a52ea61a2edfe6789a15a6929628. OSDeploy fixed-stage/input contract accepted at 6ee5bf08eed76b20e17056df9b2d27bbc1581113 on macOS/Linux; see osdeploy-contract-acceptance.md. Durable workflow binding remains pending |
| Callback compatibility and server-side binding | Pending run/attempt/identity binding, replay, duplicate, conflict and late-result proofs |
| Durable OSDeploy registration | Source/macOS accepted at53272397cdd386ec05b7ac6332780ba9ef15e62d; actual Linux database/runtime and artifact proof now accepted at f412f2c9d577081985ce503773dfa8226567f099. See linux-owned-fixture-acceptance.md. Registration remains declaration-only, not durable execution |
| Owned Linux database fixtures | Reviewed source a36c166 passed the full Linux gate at f412f2c:361 ordinary+10 documentation executions,45 included Linux cases,17 separate Python checks. Exports, independent metadata checks, owned cleanup and2,534-file seal verified; see linux-owned-fixture-acceptance.md |
| Native OSDeploy vertical slice | Provisioning libraries accepted through849766d; exact historical limits remain in provisioning-port-acceptance.md. Registration and the current four-package Linux database proof are accepted separately above. Next: durable execution, callback/service integration and recovery. No service acceptance follows from this fake chain |
| Guest-action identity contract | Additive `GuestActionIdentity` now admits exactly the five guest stages with stable `step_id == operation_id`, original attempt identity and fixed `retry_count=0`; 3 focused tests plus full adapter suite pass in `callback-contract-gap.md`. Session arming, authenticated exposure/results and service reachability remain pending authoritative callback decisions |
| Agent/build-host, CloudOSD and legacy WinPE | Pending compatible contracts and workflow proofs |
| Python/Rust single-writer transition | Contract-level Python/Rust compatibility accepted in `restart-recovery-1`; actual dual-executor generation fencing, legacy-writer quiescence and handoff remain pending |
| Independent-process recovery | Missing: current restart tests are same-process controller reconstruction and `NativeFakePve` is in-memory; the durable fixture design and exact multi-crate typed-Clone seam are `durable-fake-pve-ipc-recovery-design.md` and `fixture-capability-seam-patch-plan.md`. Controller wiring and A/B worker recovery remain open |
| Release-candidate assurance | Pending full differential/fault/rebuild/backup-restore/rollback evidence, exact immutable artifacts, remaining contract suites and independent readiness review |
| Disposable non-production proof | Separate authorization required; exact artifact, isolated stack and sacrificial workflow targets |
| Production readiness decision | Still not ready: full Linux and contract-level recovery are accepted, but complete service-process recovery, single-writer handoff/quiescence, rollback/export, operator acceptance and non-production evidence remain; deployment/cutover separately approved |

- Commit `0108405` adds a strict `FixtureCloneReads` supervisor seed schema for node status, storage, bridges, and complete cluster inventory/identities. It preserves family timestamps and explicit errors, validates fixture UUID, rejects unknown fields, duplicate VM IDs/resource names, malformed identities, and oversized payloads, and treats missing files as unavailable. Two focused schema tests, 25 default library tests, formatting, and strict feature Clippy pass. This is schema/loading only; daemon startup wiring, transport, full config/media/task facts, typed projections, and `ProvisioningFakePort` remain open.
- Commit `716c149` wires `FixtureCloneReads` into daemon startup and `FixtureReadClient::clone_reads(expected_fixture_id)`. Missing seeds return `None`; populated replies preserve timestamps/errors; fixture identity and unknown fields are checked; reads cannot append attempts. Serial feature daemon tests (9 passed, 1 ignored), default daemon tests (7 passed, 1 ignored), strict Clippy, and formatting pass, including two daemon restarts and startup immutability/corrupt-seed proofs. A parallel feature run exposed an existing `NotConnected` flake in the read-client effect test; serial rerun passed, so concurrency stability remains a qualification concern. Provisioning adapter and controller round-trip remain open.
- Commit `84c7213` adds an identity-bound `FixtureProvisioningReads` seed and read-only client transport for source/target provisioning configuration, independent power observations, and deployment/driver media inventories. Strict validated config/media deserializers are reused; fixture, operation, digest, node, VM IDs, embedded timestamps, explicit errors, unknown fields, duplicates, and size limits are checked. Missing seeds are unavailable. Initial schema checks, feature Clippy, default checks, and formatting pass; populated payload and restart/client integration proofs are still pending.

- Commit `8707163` fixes the parallel fixture transport flake: accepted Unix sockets inherited listener nonblocking mode, so bounded reads could see `WouldBlock` before an async client finished sending. Accepted streams now switch explicitly to blocking mode while retaining deadlines and frame limits. Thirty consecutive 16-thread feature runs passed; updated feature serial/parallel and default parallel suites, formatting, and strict Clippy passed. This qualifies macOS framing reliability; latest-source Linux and controller/process takeover remain open.

- Commit `c296ce1` completes the populated provisioning-read proof: source config/media round trips and invalid node/VM, timestamp, duplicate/cross-storage media, and zero-error-timestamp cases are covered. Daemon/client tests prove missing-seed unavailable behavior, all six identity fields, startup immutability, two restarts, corrupt replacement failure, and zero mutation attempts. Feature library (30), daemon (11 + 1 ignored), populated schema (2), and default daemon (8 + 1 ignored) tests, strict Clippy, and formatting pass. Target config/power remain explicit errors; no provisioning capability or controller round-trip is claimed.
- Commit `05fbc27` proves populated target configuration and power semantics. Present targets retain identity and an independently timestamped running-power observation; explicit target absence preserves unavailable power without synthesizing `stopped`. Wrong identities, timestamp mismatches, invalid power, unknown/duplicate fields, and contradictory absence payloads fail closed. Feature schema (4), feature daemon (12 + 1 ignored), default library (25), default daemon (8 + 1 ignored), strict Clippy, formatting, two-restart, startup-immutability, and zero-ledger-attempt checks pass. No provisioning adapter or controller round-trip is introduced.
- Commit `2697867` adds executable admission regressions for two facts still absent from the seed adapter: Clone template admission requires `VmPowerStatus.locked == Some(false)`, and complete inventory requires per-identity `ProvisioningCoverageV1`, not only global inventory coverage. Missing either fact prevents `Ready`; the complete baseline remains admitted. Feature admission tests (5), targeted strict Clippy, and formatting pass. The patch plan now records these required facts and the supervisor checkpoint prerequisite; no adapter or controller round-trip is claimed.
- Commit `7134591` adds strict `SeedPower { power, locked }` and timestamped source/target `ProvisioningCoverageV1` observations. Missing lock/coverage and invalid values fail decoding; `Partial` remains partial for the evaluator to reject. Existing startup-immutability and two-restart equality proofs cover the new fields, with feature schema (5), admission (5), daemon (13 + 1 ignored), default daemon (8 + 1 ignored), default library (25), formatting, and strict Clippy passing. No provisioning adapter or controller round-trip is claimed.
- Commit `ea9c5cf` pins the remaining inventory projection gap with an executable regression: identity-only `SeedIdentity` entries cannot form `ClusterVmInventory` because each entry requires its own power/status at the inventory timestamp. Source/target power cannot substitute for unrelated inventory members; stopped/running entries otherwise share the same identity shape. Focused test, strict targeted Clippy, and formatting pass. The patch plan now requires complete per-entry status before any adapter capability.
- Commit `ab373ba` adds explicit timestamp-bound `SeedRead<PowerState>` to every inventory entry. Missing/invalid status, mismatched or zero timestamps, unknown/duplicate fields, and unavailable-state violations fail closed; unrelated VM running status survives startup immutability and two daemon restarts with zero ledger attempts. Feature library (30), daemon (13 + 1 ignored), inventory schema/projection (2), default library (25), default daemon (8 + 1 ignored), strict Clippy, and formatting pass. No provisioning adapter or controller round-trip is introduced.
- Commit `bd2a39c` pins the remaining identity projection gap: `provisioning_identity` is collected for every inventory member, but coverage is currently seeded only for source/target. Omitted per-entry coverage is rejected; `Partial` and `Complete` remain distinct states and cannot be inferred for unrelated VMs. Feature inventory projection (3), targeted strict Clippy, and formatting pass. The patch plan now requires timestamped per-entry identity coverage plus separate checkpoint IPC before the adapter.
- Commit `3e64b8f` adds required timestamp-bound `SeedRead<ProvisioningCoverageV1>` to every inventory identity. Complete, partial, and explicit error states are preserved; missing/invalid/contradictory values, unknown/duplicate fields, and zero or mismatched timestamps fail closed. Projection and daemon startup-immutability/two-restart tests include unrelated-member coverage and zero ledger attempts; feature/default suites, strict Clippy, and formatting pass. The inventory seed now covers this fact, but adapter and checkpoint IPC remain open.
- Commit `b05747c` adds the feature-gated sealed `FixtureProvisioningPort`, composing all currently seeded Clone collector reads with typed Clone mutation. It preserves fixture/request/node/VM/digest and observation-time/error semantics, rejects non-Clone actions, and returns explicit unavailability for inherited unseeded reads (`vm_config`, `task_status`, `storage_content`, `qga_ping`, `native_vm_config`, unknown storage/power/configuration). Feature library tests (32), feature doctests (23), default/feature Clippy, and formatting pass; a negative contract proves it is not a `ControllerFixturePort`. Populated daemon-to-adapter round-trip, supervisor checkpoint IPC, and controller integration remain open.
- Commit `3e171f3` adds owned, fallible checkpoint IPC: supervisor arm/status/release and worker enter/poll are bound to generation, owner, operation, and `DispatchCommitted`, with bounded deadlines and state persisted before acknowledgement. Focused subprocess proof covers pause, entered/release, stale-owner rejection, restart/rebind invalidation, timeout, readiness handshake, and zero ledger attempts/effects; feature daemon (14 + 1 ignored), default daemon (8 + 1 ignored), strict Clippy, and formatting pass. `ControllerFixturePort` integration remains gated because its current checkpoint method returns `()` while the safe client is fallible; swallowing failure would permit unsafe dispatch.
- Commit `3950bc4` makes the checkpoint seam fallible end-to-end. `ControllerFixturePort::controller_checkpoint` returns typed `Unavailable`, `TimedOut`, or `Rejected`; NativeFakePve succeeds only after release; FixtureCheckpointClient maps transport failures; and `OsDeployController::advance` stops before submission on checkpoint failure/cancellation. Feature controller (6), default controller (5), checkpoint integration (2), both crates' strict Clippy, and formatting pass. The regression is pre-PostgreSQL; full controller Clone and process recovery remain open.
- Commit `e7c64b7` composes `FixtureProvisioningPort` with the owned `FixtureCheckpointClient` through `ControllerFixturePort::with_checkpoint`. The adapter validates exact operation/generation/owner binding, fails closed when unconfigured, and preserves typed IPC failures. The constructor doctest reaches `OsDeployController::new_fixture`; the daemon release/restart ownership test crosses the composed trait object. Focused feature tests, controller tests, 23 pve-port doctests, strict Clippy, and formatting pass. This is a local adapter milestone; PostgreSQL controller round-trip, independent process takeover, and full service compatibility remain open.
- Commit `bef1e21` adds the first isolated PostgreSQL/IPC Clone composition proof. An independent PostgreSQL pool observes the durable dispatch before any receipt; the owned checkpoint enters while the daemon has no accepted effect; supervisor release permits one consuming submission through `FixtureProvisioningPort`; the daemon-generated typed Clone receipt/effect are persisted; a fresh store reload returns the exact receipt; and duplicate submission is refused. The new integration target passed 35/35 (including shared owned-fixture checks), with focused rerun, strict Clippy, and formatting passing. This invokes preflight/request admission through the existing native scenario helper rather than `OsDeployController::run_osdeploy_once`; full controller collection/reconstruction, independent worker takeover, and sixteen-stage service compatibility remain open.
- Commit `179f4a0` adds an independent-process recovery-reader proof. A fresh child executable opens its own PostgreSQL connection, reconstructs durable operation/request inputs, reads the exact accepted effect through fixture IPC, verifies receipt equality and submission sequence one, rejects a mismatched digest, and leaves the parent attempt count unchanged. The targeted parent/child proof, strict Clippy, and formatting pass; details are in `fixture-independent-recovery-reader.md`. This is independent reconstruction/read evidence, not worker-A crash plus worker-B lease acquisition; that supervisor-owned takeover harness remains open.

## Current resource ruling

After the resource hold, the user reported freeing over100GiB. Main verified113,433,292KiB available on resumption and113,030,064KiB at the storage-admission review checkpoint. The local8GiB guard is cleared; no further cleanup was performed by this worker. These are point-in-time checks, not a promise of future headroom. Recheck the separate18GiB Linux artifact-start guard before that later build. Historical cleanup evidence below remains unchanged.

The user approved clearing only this isolated worktree's rebuildable Rust cache after preserving its verified executable and logs. Two approved cleanups have now occurred: first the isolated incremental cache, then the full isolated `rust-controller/target` after Task7's 118-test durability gate completed. Verified service/test executables and proof records were preserved under `.superpowers/preserved-artifacts/rust-cache-20260906.URwnzp/` and `rust-cache-task7-20260906.RRmx0J/`; original evidence outside target remains intact. The second cleanup raised observed available storage from 4,163,608 to 17,089,272 KiB. After remaining gates, the last Task7 observation was 8,921,448 KiB, not a live guarantee or satisfaction of the 18 GiB Linux artifact-start guard. Recheck before further builds.

A separate shared Docker fixture leak is confirmed: exact owned-container volume mount/unmount events and a subsequent detached-volume inspection prove that its anonymous PGDATA volume survived container removal. Container absence is not database-volume deletion proof. No existing volumes were deleted or attributed merely from inventory counts. Before Task8 broad tests, implement and independently accept fixed bounded tmpfs storage admission and owned lifecycle/capacity proof under `docs/superpowers/specs/2026-09-06-owned-docker-fixture-storage.md`. This prevents future leaks, does not reclaim existing storage, and preserves PostgreSQL defaults and existing test bounds. Keep evidence, accepted images, unrelated worktrees and Docker resources intact.

## Active implementation checkpoint

Saved OSDeploy plan restoration is accepted at `64dc3664f1ee4a3914439db9c2d433abae73c146`: independent review found no issues; macOS evidence records 48 integration and 6 compile-fail documentation tests plus formatting and strict workspace Clippy. Atomic sixteen-stage registration, complete reload and generic scheduler isolation subsequently passed source/macOS review at `53272397cdd386ec05b7ac6332780ba9ef15e62d`, including 231 baseline executions and focused covering reruns. These are validated persistence/registration results, not durable execution or Linux service proof.

The bounded Linux database backend is accepted through actual four-package execution, exact-source image checks, exported evidence, independently hashed OCI metadata and verified owned cleanup. See `linux-owned-fixture-acceptance.md`; topology feasibility is no longer the only Linux SQL evidence. This is a registration/provisioning prerequisite, not a new service candidate. The durable-phase schema, private reconciliation proof, bounded expiry scan and consuming-send policy have source-backed design review. The nine-task implementation plan and binding specifications were committed at `62e8187e4de20d25d3aee52ca72c49e817df09c9` in `docs/superpowers/plans/2026-09-05-rust-osdeploy-durability.md` and `docs/superpowers/specs/2026-09-05-rust-osdeploy-durability/`. Task 1 is dispatched to a fresh Astra source owner for the additive schema and owned failure/cancellation rollback tests; no implementation acceptance is claimed yet. Task-level source review, full macOS checks and a fresh exact-source Linux gate are required for this phase. Next come callbacks, full sixteen-stage service integration and recovery; the overall goal remains active.

Reliability evidence to retain: the first fixture-change macOS suite reported two native-controller failures, then stranded at an existing unbounded checkpoint wait and was terminated as incomplete. The first failing test subsequently passed unchanged in isolation; the complete native harness passed 44/44 unchanged. The replacement full macOS suite later passed296 executions before the supervisor repair; final Linux passed371 afterward. These results do not establish the original cause. Preserve the original failure/interruption and later results separately; do not call this a fixed defect or erase it from release-candidate assurance. Before PoC readiness, revisit this uncertainty with full service/recovery evidence and bounded diagnostics for early controller exit before a test checkpoint. Evidence and exact owned-resource cleanup remain in the Linux fixture SDD ledger.

The rich provisioning plan separates broad observed/declaration values from executable compatibility. Its next request layer must retain the original source and target before-state, bind the full workflow and individual operation separately, preserve the original dispatch across recovery, and never turn missing receipts into resend permission. PE start and installed-disk start remain distinct operations.

Main's pinned upstream check corrected the new resize worker convention and added its before-config digest. Configuring an existing disk's serial must preserve capacity, not model informational size as growth. Desired disk serials longer than 20 ASCII bytes must be rejected at request construction and downward workflow conversion before any clone; never silently truncated. These are conservative contracts for the next local phase, not proof of compatibility with the installed production PVE version. Task 1's broad validated fact representation remains unchanged.

Upstream references pinned to qemu-server commit `6c0127e612f6c576888a13f9bfb30874911b804d`: [resize endpoint implementation](https://github.com/proxmox/qemu-server/blob/6c0127e612f6c576888a13f9bfb30874911b804d/src/PVE/API2/Qemu.pm#L5558-L5685) and [disk size/serial schema](https://github.com/proxmox/qemu-server/blob/6c0127e612f6c576888a13f9bfb30874911b804d/src/PVE/QemuServer/Drive.pm#L181-L235).

## Completion truth

Task8 fresh local requalification is accepted at surviving source `730d296d7d61671a3c11c461fda5c39ea5670310` after independent Astra evidence review. Fresh serial gates passed controller64, native59, durability127, store-library47, registration50, and20 compile-fail documentation cases; corrected unit coverage passed5; formatting and strict offline/locked workspace Clippy passed. Complete stdout/stderr and bounded supervisor receipts are retained in the restart worktree under `restart-task8-*`; the current source seal covers all11 Task8 source paths. Main independently observed the exact owned native-proof inventory empty after each completed database gate. This is fresh local evidence, not recovery of the deleted historical Task8 reports, RED/failure/cleanup/stack artifacts or source seal. The old historical failures and limitations remain unrecovered and must remain explicit in any readiness assessment. Task8 proves the approved owned send/observation/drain prefix only; Linux exact-source qualification, full sixteen-stage callback/service workflow, independent-process recovery, actual Python/Ansible writer quiescence and production readiness remain open. Production `192.168.2.4` and real Proxmox remain read-only.

Owned Docker storage qualification deliverable B is accepted for local test infrastructure at source `f7c900020ba55370f92f8d5cf9898fe53e9cf9eb`, reviewed complete range `bf715c2dda806192f03771dc9bebe5d25e556ee0..29be6db9602795baea126dd5c6545ba07fb02a03`. An existing Astra reviewer uninvolved in B design/implementation was reused under the explicit allocation fallback after new-agent creation was refused; both independent specification and code-quality verdicts PASS, no actionable findings. Main inspected the complete report, source and relevant receipts, verified168manifest hashes/byte lengths, exact tested source/observer hashes and clean HEAD, and independently passed formatting and full-range whitespace checks. Dedicated SQL/explicit cleanup, Drop and current-thread cancellation plus the four selected heavy gates passed; eight original full/service gates passed410executions (204repeated shared-helper executions,206harness-specific bodies). Both service cases used actual owned PostgreSQL with unchanged internal55/85second bounds. Full gates retained849samples/246fixtures; focused heartbeat separately retained11samples/1fixture. Independent raw-evidence audit confirmed all860focused/full samples had five successful bounded reads before Cargo terminal, and247unique fixtures had494exact-ID/anchored-name absence selections (407original plus87later).

The accepted helper/audit uses fixed1GiB tmpfs, at least128MiB reserve at qualified observations, default PostgreSQL settings and ordinary exact-owned cleanup. This prevents the diagnosed future anonymous-PGDATA fixture leak; it does not delete existing volumes or prove continuous peaks, complete event history or persistent-storage recovery. Preserve original SQL/history failure without its original failed projection, first interrupted durability attempt with unknown cause, heavy rollback93of97retained samples (including incomplete preceding reads for the first post-gap sample), and corrected durability87lost original cleanup receipts/43lost aggregates. Later exact absence checks are later state, never recovered original evidence. Final preflight observed100779606016bytes free, unchanged cached image, all four protected containers exited and no running labeled fixture; no production access or volume cleanup. This supersedes older B-pending/resource holds below. Task8 controller integration may proceed after its committed lifecycle/lock-metadata clarifications; Task9 fresh exact-source macOS/Linux, full sixteen-stage callbacks/service, independent-process recovery and actual Python-writer quiescence remain open. Goal remains active; production and real Proxmox stay read-only.

Owned Docker storage admission deliverable A is accepted at `86dc63172c74eb7343a85c7d9d6f429972fd094e`. Fresh independent Astra specification and code-quality review passed with no actionable findings. Main read the complete482-line report and complete review, verified the tested one-file source hash and clean exact commit, and independently passed formatting and exact-range whitespace checks. Two compiled behavioral RED assertions preceded the repair; subsequent4 storage and30 shared-helper executions passed (34 executions,30 distinct bodies, five new bodies). Strict offline/locked workspace all-target/all-feature Clippy passed. Existing deliberate synthetic cleanup/panic diagnostics are retained and explained, not represented as live cleanup failures.

The helper now requests fixed1GiB PGDATA tmpfs and refuses unexpected storage after existing ownership verification, before yielding a database connection string. Original finite process/cleanup bounds, exact-container-only removal, PostgreSQL defaults and Linux receipt backend are unchanged. This is source and non-live acceptance only: actual Docker filesystem/capacity, SQL workload behavior, normal/Drop/current-thread cancellation lifecycles, exact absence/volume-event evidence and affected live regression suites remain mandatory deliverable B. Broad fixture runs and controller Task8 remain gated on that qualification. The production-candidate goal remains active; production and real Proxmox remain read-only.

Durability Task 7 recovery, original-dispatch reconciliation, all-sixteen cancellation, and bounded due/repair/immutable-expiry maintenance is accepted at `7687397f7a86b1c53fb6bddb3b2ffe32cc060007`. Fresh independent Astra specification and code-quality review found no actionable defects. Main read the complete 3,334-line implementation report and 55-line review, matched the twelve changed source hashes against the during-test seal and exact commit, verified clean HEAD, and independently passed formatting and full-range whitespace checks. Final serial macOS gates passed 118 durability, 38 store-library, 55 native-persistence, 50 controller-native, and 52 scheduler executions: 313 runtime executions /213 distinct bodies after100 repeated shared-helper executions. Nineteen intended compile-fail contracts are separate; strict workspace all-target/all-feature Clippy passed. The review is static independent assessment of source and recorded evidence, not a second runtime run.

Actual original-attempt reclaim/repark and terminal residual cleanup preserve immutable deadlines and exposure; original-dispatch Unknown reconciliation never creates resend authority. Cancellation settles all sixteen registered members atomically without manufacturing attempts for unstarted work. Due discovery and projection/deadline maintenance use distinct bounded scans; poison and deferred-visibility tests demonstrate progress and wraparound without changing immutable clocks. Representative row-write and deferred-commit failures preserve durable snapshots. Two additional stack aborts in the recovered Clone→Capacity→ConfigurePe chain are retained: narrow preflight future boxing and the final full suite prove that observed composition, not universal stack safety. The earlier timeout, abort and watchdog limitations below remain open assurance context.

The confirmed shared-fixture storage leak is a separate prerequisite, not a Task7 source defect or an accepted fix. No further broad fixture suites precede its repair and owned capacity/lifecycle proof. Then Task8 controller admission/drain integration and Task9 fresh exact-source macOS/Linux gates resume. Callback compatibility, all-sixteen service execution, independent-process recovery and actual prior-writer quiescence remain open. This supersedes earlier next-task statements below; the production-candidate goal stays active and production remains read-only.

Durability Task 6 durable outcomes and same-attempt Waiting resume is accepted at `f9a45e26f534045aa23bf652d812e081f9c92bb5`. Fresh independent Astra specification and code-quality review found no actionable issues. Main read the complete 1,880-line implementation report and complete independent review, matched all nine tested source hashes, confirmed clean exact HEAD, and independently passed formatting and full-range whitespace checks. Final serial local checks passed 94 durability, 38 library, 25 pure evaluation and 23 fake executions: 180 executions /155 distinct bodies after25 shared-helper repeats;19 compile-fail contracts are separate. Strict workspace Clippy passed. Actual growth/no-growth Clone→DiskCapacity→ConfigurePe histories now prove selected physical predecessors, two versus three sends, original selected-time reload, synchronous response loss, Waiting/due/resume without a second attempt, and atomic terminal/park/expiry rollback. Original replay remains behind current authority and cancellation admission; it restores no permission across those fences. All later thirteen stages still reject execution.

Preserve Task6 assurance limitations: six historical debug stack aborts required narrowly scoped future boxing, with final composition passes but no universal stack claim; one isolated unchanged-bound fixture connection timeout remains unexplained despite exact retry and full-suite passes; an initial unbounded compiler check failed and is a watchdog deviation, not runtime proof. Exact owned cleanup and historical receipt limitations are retained in the SDD record; four unrelated stopped containers were preserved. Task7 public recovery/reconciliation/cancellation and bounded due/deadline repair is next. Full callbacks, sixteen-stage service execution, independent-process recovery, fresh exact-source Linux and actual prior-writer quiescence remain open. The production-candidate goal remains active and production remains read-only.

Durability Task 5 committed consuming dispatch and original response capture is accepted at `8a140f6c375f6e27413d74d31263ab6e20c56563`. Fresh independent Astra review found no actionable specification or code-quality issues. Main read the complete 1,964-line implementation report and 61-line review, matched all seven tested source hashes and clean exact commit, and independently passed formatting and full-range whitespace checks. Final serial macOS verification passed 77 durability plus 37 library executions (114 executions, 89 distinct bodies after 25 shared-helper repeats), with 19 supplementary compile-fail documentation checks and strict workspace Clippy. Genuine Clone submission/capture now proves immutable first receipt time, original evidence replay after receipt arrival, observational late capture and no replacement permit after drop, cancellation, errors or response loss. Both behavioral RED/GREEN chains and the separate compiler correction are retained. Task 6 must establish genuine growth/no-growth selected-success history, synchronous ConfigurePe capture, atomic outcomes and same-attempt parking/resume. All-stage cancellation/recovery, controller admission/drain, fresh Linux, full sixteen-stage service, compatibility and single-writer proofs remain open. This is not production readiness or deployment approval; the full goal stays active.

Durability Task 4 physical evidence/history binding is accepted at `4608b4aea43a7e97464d67496e10b65fee5056b9`. Fresh independent Astra review found no actionable specification or code-quality defects. Main read the complete implementation and review reports, verified all seven tested source hashes and clean exact commit, and passed formatting/whitespace checks. Final serial macOS verification passed63 durability+37 library=100 executions/75 distinct bodies, plus6 supplementary documentation checks and strict workspace Clippy. Original compiled RED, five stack aborts, failed caller-only mitigation and subsequent production future-boxing repair are preserved separately; the original unboxed replay caller passes without stack-limit changes. Typed provenance, immutable original fences, equivalent semantic replay and full historical request reconstruction are implemented, but advice grants no send permission. Corrected older fixtures are structural history only. Task 5 must prove actual consuming dispatch/original receipt capture and replay after receipt arrival; Task 6 must prove genuine selected-success growth/no-growth history and parking/resume. This is not new Linux, full-service recovery, single-writer or production readiness acceptance. The full goal remains active; production remains read-only.

Durability Task 3 lifecycle is accepted at `0585b2d9962c30ca730c9538cb061b0175c11f1c` (initial implementation `d678bfaefa55e27da536afe699fdc4c16acdbab7`). Actual bound activation, first start, heartbeat and read-only continuation preserve one attempt and original scope deadlines under locked authority. Independent review's sole P2, loss of an opened scope during unactivated cancellation classification, has preserved compiled behavioral RED/GREEN and a clean specification/code-quality re-review. Main read complete original/fix reports and reviews, verified exact source/report hashes and clean source HEAD, and independently passed formatting and exact-range whitespace checks. Final macOS verification passed 37 library + 56 durability + 52 scheduler executions (145 executions, 95 distinct bodies after shared-helper deduplication), plus six supplementary documentation checks and strict Clippy. Private exceptional constructors are not end-to-end recovery acceptance: original-dispatch reconciliation, inherited-anchor consumers, Waiting resume and recovered-Pending claim retain their recorded later-task gates. Task 4 physical evidence/history binding is next. Changed-source Linux, full sixteen-stage service/recovery, independent single-writer handoff and production-candidate readiness remain incomplete; the overall goal stays active and production remains read-only.

Durability Task 2 strict execution-history restoration is accepted at `62f423c11ac46ea9c2addece94986c85bea6d66c` (initial implementation `65d17300838672c02a08376ceb22207612213999`). Independent review found two P2 issues: first-start transaction adjacency/time binding and an ended evaluator lease surviving ordinary Waiting parking. Both have preserved behavioral RED/GREEN evidence and a clean independent fix re-review for specification and code quality. Main read the complete original and fix reports/reviews, checked the exact two-path fix and matching source hashes, and independently passed formatting and whitespace checks. Final unchanged-source macOS verification passed6 library+47 durability+41 registration=94 executions (69 distinct bodies,25 shared repeats), plus5 supplementary compile-fail documentation tests and strict Clippy. Later resumed Waiting/Running and exactly linked terminal residual leases remain accepted. This proves strict structural history restoration, not physical evaluator reconstruction, lifecycle execution, changed-source Linux or full service/recovery readiness. Task 3 activation/start/heartbeat/continuation is next; the full goal remains active.

Durability Task 1 is accepted at `93f54c792c47e9d7dfcc1380fd297a3f7d5f196a`: nine-table schema and SQLx-owned migration transaction. Main read the complete implementation and fresh independent review reports; specification and code-quality verdicts are both clean, with no actionable findings. The macOS durability33/33 and registration41/41 results represent74 executions and49 distinct test bodies, with25 shared fixture tests repeated. SQL failure and cancellation inside0005 demonstrate rollback and same-backend clean reuse. The initial missing-table RED and incorrect SQLSTATE test expectation are retained separately. Main independently passed formatting and exact-range diff checks. This supersedes the earlier dispatch checkpoint above; Task 2 strict execution-history restoration is next. Task 1 acceptance is not new Linux, execution or service proof.

Next implementation target is a service-driven OSDeploy vertical slice built on the existing journal/scheduler, not another independent execution stack. Source research identifies distinct PE/disk start operations, durable callback waiting beyond worker leases, disk-capacity versus free-space separation, explicit firmware/media/identity representation, and legacy callback transaction boundaries. Nominal CloudOSD run-detail and bootstrap-claim GETs can write; do not use them as production read-only probes.

The local vertical sequence is fixed at sixteen stages, including actual disk growth, both media configurations, conditional force-stop with retained grace-timeout history, all base guest actions, independently authenticated agent heartbeat and host-side QGA. Current client response-loss recovery is a separate limitation: a lost successful `/next` claim response cannot be treated as permission to redeliver an executable action. Full service/fake-world restart proof remains mandatory after the input phase.

Do not mark this goal complete from test totals, fake-PVE success, HTTP health, or OS installation alone. Report which readiness level is proven and what requires external approval. Native operational readiness, OOBE, enrollment, ESP and usable-device acceptance remain separate facts. This PoC goal does not imply the full replacement programme, production cutover, or Ansible retirement is complete.
