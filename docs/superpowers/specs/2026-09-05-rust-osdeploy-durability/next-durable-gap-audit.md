# Next durable OSDeploy phase: bounded gap audit

2026-09-05. Read-only preparation against source `f412f2c9d577081985ce503773dfa8226567f099`. Read the complete registration SDD `next-durable-schema-proposal.md` (including the selected send/capture amendment), `next-durable-main-decisions.md`, and `next-durable-transaction-boundaries.md`, plus narrowly relevant current store/scheduler/migration and provisioning evaluator code. Main's decisions and the later amendment supersede earlier provisional wording. No implementation authorization is inferred.

## Outcome

No demonstrated contradiction requires reopening the selected one-attempt, strict-deadline, one-shot permit/separate original-response-capture, or cooperative send-admission choices. The initial relational proposal can be inserted with its stated immediate FKs. The concrete gaps below must be resolved in the implementation brief or its implementation; they are not claims that current registration already executes this workflow.

### Main acceptance still required

1. **Freeze the proposed initial schema, exact wire variants, errors and polling constants.** Main-decisions line 29 still requires this acceptance; schema lines 92 and 128 propose concrete values, not alternatives that need reinventing. Freeze the full initial action set, including `lease_expired_uncertain` and the explicit null-attempt allowance for `run_cancelled` in schema lines 78–80. The older transaction amendment's two-variant null-attempt restriction is superseded by this later, necessary cancellation-control case. Acceptance should name the superseding proposal/amendment so an implementer does not follow the stale narrower check.
2. **Specify the bounded immutable-scope sweep contract.** Schema line 116 gives keyset/wrap rules for Waiting/Unknown projection repair, but only says an additional immutable-scope scan finds elapsed members. Immutable expired scopes remain forever, so a fixed first-32 deadline query can repeatedly select already adjudicated history and starve later scopes. Pin candidate eligibility, stable cursor/wrap, per-sweep bound and handling of an invalid candidate before acceptance. Use the proposed batch bound; do not increase phase budgets or turn discovery into authority. This is an operational scheduling contract still missing, not a change to expiry semantics.

### Implementation details under the selected design

3. **Add the missing family-private original-dispatch reconciliation proof.** Schema line 114 permits Unknown→Satisfied/Failed/Conflicted; the proposed `OsDeployTransitionProof` in transaction-boundaries line 131 names expiry/reclaim/repark/cancellation cases but no reconciliation case. Current domain rejects every transition from terminal Unknown (`controller-domain/src/transition.rs:43`); current store supports the transition only through `TransitionPolicy::NativeReconciliation` (`postgres-store/src/scheduler.rs:887`, `:1111`). There is no existing OSDeploy hook to invoke. The implementation needs an OSDeploy-only proof reconstructed from the selected Unknown, original admitted dispatch/attempt/evidence, current authority/CAS, no cancellation/residual lease and strict original deadline. It must not route OSDeploy through the native proof policy or broaden `DomainSignal`. Same-state Unknown expiry/scheduling should append its decision without a state transition. This implements the selected policy; no new terminal-repair policy choice is needed.
4. **Write an explicit insertion schedule; deferred typed FKs do not defer journal/attempt FKs.** Foundation already supplies AT and EV composite unique keys (`0001_foundation.sql:45`, `:75`); registration supplies OP (`0004_osdeploy_registration.sql:17`). Initial activation can be: attempt → activation journal/outbox/decision → deadline → binding → lease-acquisition journal/outbox/decision → epoch → worker lease → state event/projections. Dispatch needs its journal/typed decision, existing typed preflight and existing epoch before the dispatch index; receipt needs its journal before receipt index. In particular, epoch→binding and every EV remain immediate. The proposal's blanket mention of insertion cycles does not require a cycle here: self-reference in a JSON detail is not an immediate SQL FK. Name any actually deferred constraint in the migration and prove rollback/replay ordering. The future atomic grace transaction must separately order PeComplete decision/anchor before the grace binding and cannot rely on deferred typed FKs to rescue a missing immediate EV/AT target. No foundation constraint change is indicated.
5. **Do not reuse generic lifecycle or native persistence shortcuts.** Generic claim creates a fresh five-minute attempt (`postgres-store/src/scheduler.rs:162`); native decision append uses native revision-based keys and a native snapshot (`scheduler/native.rs:528`); public generic `append_event` accepts observation events only (`store.rs:285`). The new family needs atomic typed provenance-index plus journal/outbox/projection writes, action-specific replay checks, one immutable attempt binding and fresh lease epochs. The current registration loader validates declarations, not execution history (`osdeploy/records.rs:7`); compose it with a new strict execution reload, preserving its existing public contract. The scheduler crate is a facade (`scheduler/src/lib.rs:8`), so these private transactions belong beneath the existing store-backed scheduler, not a parallel executor.
6. **Keep selected-state proof distinct from the latest aggregate event.** Activation/start/lease-control decisions have null resolution; dispatch has ready; observations/receipts advance revision without replacing selected terminal proof. Generic `rebuild_projection` only replays execution-state events (`store.rs:422`) and cannot reconstruct the new schedule, provenance, basis or unavailability count. Implement the separately proposed validated decision/control replay; test receipt insertion and heartbeat between evidence capture and decision CAS, plus Unknown→Unknown deadline closure. Do not interpret the highest decision of any kind as success or use projection watermarks as evidence fences.
7. **Pin two easy-to-miss first-prefix cases in the tests, without changing policy.** A preflight `ObservationUnavailable` produces Unknown, not Waiting; the selected reconciliation route requires an original dispatch, so an undispatched Unknown gets no automatic recovery/send loop (schema lines 78, 112). The 2/4/8/10-second unavailable backoff is not permission to reopen it. Conversely, a stopped VM already at effective capacity yields preflight Satisfied/ObservedNoChange with no dispatch (`pve-port/src/provisioning/evaluation.rs:304`); baseline restoration explicitly supports this preflight mode (`evaluation/context.rs:194`). ConfigurePe must accept that exact selected historical baseline without inventing a capacity request, receipt or permit. These are consequences of completed evaluator behavior, not new choices.

## Compatibility and remaining goal

The proposed authority→run→family-cap→sorted operations→leases/projections order is compatible with the inspected registration/native paths when followed literally; generic observation append takes only its operation lock and does not later acquire run/authority. No reversed lock cycle was demonstrated by this bounded inspection. Preserve native/generic guards and policies, the declaration-only registration output, all sixteen stage IDs and seven PVE mappings. The consuming permit plus non-authorizing capture amendment is already selected; do not reopen its signatures or restore a permit after restart, cancellation, timeout or ambiguous send.

StartPe and later execution remain closed until actual callback/session arming and their stage dependencies exist. Atomic truthful grace activation with selected PeComplete success remains mandatory in that later phase. The first three FakePVE stages are an intermediate proof only: full sixteen-stage service execution, guest compatibility, independent-process restart/fault/recovery and readiness assessment remain the goal. A local admission/drain gate is not transport-side fencing or proof of old Python/remote-writer quiescence. Production and real PVE remain read-only.

Review dimensions: security/authority boundary appears coherent within inspected scope; correctness has the private-proof and contract-completion gaps above; performance needs the immutable-scope progress rule; maintainability favors the selected family-private module and shared persistence semantics. Positive checks include existing composite FK support, compatible inspected lock order, and the completed no-dispatch baseline constructor.

Verification boundary: inspection only, using the engineering code-review skill. No application source or frozen gate script edits, tests, builds, Docker, helper/MCP/network calls, commits, or child agents. This audit does not attest the concurrently owned Linux runtime result.
