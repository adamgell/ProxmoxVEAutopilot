# Owned Docker fixture storage prerequisite

This is a separate test-infrastructure repair required by the confirmed anonymous PGDATA volume leak. It does not change the Rust controller's production behavior, accept Task7, replace Task8, or narrow the full production-candidate goal. Implementation waits for Task7 independent acceptance and a concrete task brief pinned to an exact clean base.

## Evidence

Main's `.superpowers/sdd/2026-09-05-rust-osdeploy-durability/fixture-volume-leak-evidence.md` records a naturally observed owned container, exact Docker mount/unmount events and its detached anonymous volume remaining after container cleanup. The full `fixture-volume-leak-preflight.md` supplies source/caller/deadline analysis and qualifies historical Linux capacity evidence. Main read both completely and independently checked the current helper plus historical tuned PostgreSQL arguments.

## Selected requirements

- Prevent persistent anonymous PGDATA allocation for newly created Docker fixtures using the fixed compiled option `/var/lib/postgresql/data:rw,nosuid,nodev,size=1g,mode=0700` passed with `--tmpfs`. No caller/environment-selected path, options, size or mount.
- 1GiB is the first current-default validation candidate, not established adequacy. Require at least128MiB available at observed workload checkpoints, no observed ENOSPC/OOM, and all selected existing workload assertions and time bounds unchanged. Report sampled capacity honestly; do not claim sampling captures every possible future peak.
- Preserve PostgreSQL defaults, including existing durability settings. Do not reduce buffers/WAL, disable fsync/synchronous_commit/full_page_writes, force checkpoints for passing capacity, add a cgroup memory limit or silently enlarge tmpfs/timeouts. A failed adequacy test returns to main for a distinct decision.
- Preserve the cached image selector, `--pull=never`, local Unix Docker endpoint gate, generated exact name/label/ID ownership, loopback-only dynamic publish, and the pending cleanup guard established before create.
- Add storage admission before yielding the connection string: exact returned full container ID, exactly the fixed HostConfig.Tmpfs map, no binds/volumes/extra destination, and either empty Mounts or a single matching tmpfs entry. Reject missing/malformed/truncated/failed responses and wrong identity or configuration. Do not weaken existing ownership verification.
- Preserve setup30s, command3s, shared cleanup3s and existing bounded child cleanup allowances. New Docker reads use the existing owned process runner and remain within setup/outer budgets. No detached child or unbounded subprocess.
- Storage refusal must retain ordinary exact-container cleanup through existing identity guards; a bad mount never authorizes volume removal. Keep `rm --force <verified-id>` unchanged. Container absence alone is not proof that an unexpected volume was removed.
- The Linux receipt backend, frozen prior candidate gates, controller/store production modules, Cargo dependencies/lockfile and existing test business assertions remain unchanged.

## Permitted implementation scope

Primary paths are `rust-controller/proof_support/mod.rs` for fixed launch/storage admission and non-live unit tests, and new `rust-controller/crates/postgres-store/tests/docker_fixture_storage.rs` for dedicated owned live proofs. Keep the public Container API unchanged; any additional inspection interface is sanitized and test-only, with fixed commands/paths and no generic command or deletion interface. A narrow change to `proof_support/process.rs` requires a concrete necessity ruling before editing, not implied permission.

## Proof requirements

1. Compiled non-Docker behavioral RED for actual launch-argument selection lacking bounded tmpfs and ownership-valid persistent-volume storage being inadmissible. Do not create another leaking volume for RED. Then implement and obtain GREEN.
2. Exact negative cases for missing/wrong/unbounded tmpfs, bind/volume/extra mounts, malformed fields, failed/truncated output and wrong full ID; both admitted daemon tmpfs representations; preserve every existing ownership/cancellation/one-shot/hung-child cleanup assertion.
3. Dedicated explicitly selected Docker-only live tests with bounded setup and outer180s each: normal explicit cleanup, Drop and current-thread cancellation. At most two simultaneously owned fresh fixtures. No new live test body in the shared helper, to avoid multiplying real fixture probes across includers. Docker-only cases must not execute in the receipt-only Linux harness.
4. For those new exact-owned fixtures, verify mount configuration and actual tmpfs filesystem type/capacity; migrate and exercise committed writes, independent-pool reads and rollback while preserving defaults. Record only allowlisted settings, database sizes, filesystem capacity/free counters and bounded memory/OOM observations; never raw environment/connection secrets or data contents.
5. Confirm exact full-ID and generated-name absence after cleanup using successful bounded daemon queries. Correlate bounded volume events to those exact IDs to prove no persistent volume creation/mount for the tested lifecycle. A global unchanged count is insufficient in a shared daemon. Preserve unrelated inventory separately without deleting anything from it.
6. Only after owned smoke/capacity proof, run selected heavy current-default registration,35-member recovery and atomic rollback cases, then full affected durability/native/controller/scheduler and service fixture regression gates serially under their existing watchdogs. Record observed capacity throughout the heavy tests without changing their work to fit storage. Count repeated helper tests separately from distinct bodies. Preserve original failures and cleanup uncertainty.
7. Keep offline/locked formatting/strict checks and existing process/Linux protocol synthetic coverage. Fresh exact-source Linux runtime proof remains a later full-goal gate; prior Linux acceptance does not prove current default-profile capacity.

## Authority and non-goals

No existing-volume deletion, pruning, Docker reset, retrospective volume ownership inference, production access, key/config change or publication. This repair prevents new leaks; it does not reclaim existing leaked storage. Newly created owned fixture data remains disposable only through the approved exact-container lifecycle. Tmpfs is volatile across container stop/restart and may have host swap/backing behavior; it does not establish persistent-storage/power-loss recovery or a guarantee of zero host disk writes. Rust-controller restart proofs keep their database fixture alive, and full process/recovery/cutover requirements remain intact.

## Sequencing

Task7 source7687397f7a86b1c53fb6bddb3b2ffe32cc060007 is accepted through main tracker commit c511ca63c97b638cf8fd60fd822683d77533581d. Use a fresh Astra source owner and independent review for this prerequisite before further broad Task8 fixtures. Main must pin each implementation brief and exact clean base; this specification alone does not dispatch an implementation agent.

The prerequisite has two separately reviewable deliverables: A, fixed launch and fail-closed storage admission with compiled non-live tests; B, dedicated live lifecycle/capacity harness and affected-suite qualification. Deliverable A cannot claim runtime capacity, actual leak prevention, or permission to resume broad suites. The plan `docs/superpowers/plans/2026-09-06-owned-docker-storage-admission.md` implements A only. Main prepares B against A's accepted interface; all live requirements above remain mandatory before this prerequisite is complete. The existing 8GiB local build and 18GiB Linux artifact-start resource guards remain in effect. Source/test preparation can proceed below the local guard, but compiling RED/GREEN, builds, live fixtures and acceptance wait for a verified guard clearance; do not bypass TDD by implementing the repair before compiled RED.

## Deliverable B design checkpoint

A is accepted at `86dc63172c74eb7343a85c7d9d6f429972fd094e`, recorded by main at `78b96ce5362eb7c8b3cf98db99e1d9087ed412e5`. Both independent review verdicts passed. Main verified approximately108GiB free after the user cleared storage; no worker volume deletion occurred. B still requires a complete implementation plan and exact-base dispatch; the following fixes its design boundary, not a runtime acceptance claim.

### Source organization and read-only observation interface

Add private test-only `rust-controller/proof_support/docker_storage_probe.rs`, rather than growing launch/cleanup logic with the separate observation implementation. B may modify `proof_support/mod.rs` only for this test-only module/API bridge and sanitized test identity tracing, and create the dedicated `crates/postgres-store/tests/docker_fixture_storage.rs` harness. Those three paths are the complete B source scope; `process.rs` remains unchanged unless main separately establishes a concrete necessity. No ordinary Container start/cleanup signature or production export changes.

The test-only audit handle is constructed from the existing owned Container, never arbitrary caller-supplied Docker names/IDs/endpoints. It carries only the exact generated name, returned full ID and already validated local Unix endpoint. It exposes fixed read-only snapshot and post-cleanup observation operations; it has no cleanup/removal method, generic command interface, mount override, endpoint override or restore-from-wire constructor. Any retained/cloned handle remains read-only and cannot become deletion authority. Each live observation reverifies exact ownership where the container is expected to exist; a stale or missing identity returns fixed failure/uncertainty.

Snapshots use the accepted A storage predicate, a fixed `stat -f` query of `/var/lib/postgresql/data`, and allowlisted container/cgroup memory/OOM metadata. Require actual tmpfs, exact1GiB capacity, at least128MiB available in each observed sample, and zero observed OOM/OOM-kill indicators. Arithmetic and decoding must reject malformed, overflowed, missing or inconsistent data. No raw configuration/environment, other filesystem paths, data files or credentials are returned. Samples are checkpoints, not proof that every transient peak was captured. Do not change database work or force checkpoints to improve them.

Test-only identity tracing emits the generated pending name before create and the matching name/full ID after successful creation, without arguments/environment/DSN. This makes actual newly owned fixtures identifiable in the live workload's own output. Do not introduce background fixture tasks, periodic workers, extra cleanup I/O or altered cleanup deadlines. Heavy-workload observation is externally owned by the bounded qualification runner; it must capture those exact identities, apply existing name/label/image/full-ID checks before reads, and preserve sampling gaps/uncertainty.

### Dedicated live proof

Compile the dedicated Docker live test bodies only on macOS, and require explicit ignored-test selection there. This prevents the future Linux receipt-only include-ignored gate from accidentally running Docker proof bodies. No new live body belongs in the shared helper. Preserve existing shared synthetic tests and distinguish their repeated execution counts.

Use three explicit180-second outer cases: SQL plus explicit cleanup with at most two simultaneously owned fixtures; ordinary Drop cleanup; and current-thread cancellation of an owned pending future after a real fixture is ready. The cancellation proof is not a claim about a cancelled daemon create response; the existing synthetic lost-response guard tests retain that distinct coverage. Setup remains30s per fixture, command3s, shared cleanup3s plus existing finite child-reap allowance. Waiting for cancellation/pause must itself be bounded and cannot hide early task failure.

For the SQL case, run the actual checked-in migrations, commit probe rows, verify them through an independent pool, roll back a separate transaction and prove the committed count remains unchanged. Both fixtures use the current default launch profile. Read only allowlisted `current_setting`/database-size values; require fsync, synchronous_commit and full_page_writes remain on, record buffer/WAL/connection/version values and preserve their defaults. Numeric counts and sizes may be reported; table contents and DSNs may not.

Post-cleanup proof must obtain successful bounded exact-full-ID and exact-generated-name daemon listings that are empty. An erroring inspect alone never proves absence. Correlated volume-event inspection is supplementary to admitted Mounts and actual filesystem evidence: it must report bounded history limits and cannot silently treat missing/truncated/unavailable history as no volume activity. Main's separate event-preflight determines the precise finite history predicate before execution; no retrospective volume deletion or creation of a persistent-volume negative control is permitted.

The selected event predicate is an epoch-to-now, unfiltered retained-history query, projected by a fixed Go template into compact `[TimeNano, classification]` rows. Classification is0 for all other events,1 for this exact owned container's create,2 for its destroy,3 for a volume mount whose container attribute equals this exact owned full ID, and4 for its volume unmount. Never emit raw unrelated Action text: exec actions may contain commands. Full ID interpolation is limited to the already validated hexadecimal identity, not arbitrary caller input. At most256 compact rows fit within the unchanged8192-byte process cap; overflow/failed/malformed output remains failure.

Require at least one returned timestamp strictly before the inspected container Created timestamp, nondecreasing timestamps, exactly one correctly ordered own create/destroy pair, and no classification3/4. Missing earlier witness, endpoints or ordered history is uncertainty, not success. A full256-entry ring is not automatically incomplete when an earlier witness and both endpoints remain; a short result alone is not completeness evidence. Concurrent eviction before the snapshot may remove required witnesses and must fail closed. Clock/order anomalies are also uncertainty. This establishes only a bounded recorded-event bracket: upstream source includes pre-create volume-service activity without corresponding attributed mount-event logging, so even an admitted bracket cannot prove the absence of unlogged internal work.

Require3–256 complete JSON-line rows with positive signed64-bit nanosecond timestamps, lossless Created conversion, and Created <= own_create <= own_destroy. Do not sort records to hide observed order anomalies. The source samples timestamps before the publication lock, so even a nondecreasing returned slice and older timestamp are not a formal insertion-order/completeness certificate. Use only the exact bounded-evidence wording, never zero-volume-creation or guaranteed full-history language. The complete source-backed preflight and exact privacy-safe template are retained in `.superpowers/sdd/2026-09-06-owned-docker-storage-admission/deliverable-b-event-preflight.md`; its candidate was not executed as a live qualification. No daemon-side container filter is allowed because it would exclude volume events whose container relation is an attribute rather than Actor.ID. A missing older witness or a long/busy lifecycle can legitimately make this proof unavailable; do not generate canaries or retry indefinitely to manufacture success.

Supplement the event bracket with the fixture's exact observed image ID and a read-only inspect of that immutable image ID's declared volumes, requiring only the PGDATA declaration overridden by A's fixed tmpfs. Combine this with the accepted HostConfig/Mounts checks, actual filesystem/capacity samples and exact post-cleanup absence. Report those distinct observations rather than claiming events alone prove complete leak prevention. The primary-source preflight informs this design but is not proof that the installed daemon binary is identical to the inspected upstream commit. No Docker daemon/code/configuration mutation is authorized.

For the existing service regression portion, preserve the actual internal55-second `observation_service::authenticated_service_is_visible_but_never_execution_ready_or_a_writer` and85-second `observation_service::infrastructure_service::selected_node_service_is_independent_and_never_a_writer` bounds. Both are owned PostgreSQL tests in the `controller-service --test service` harness; their original SQL snapshots, audit logs, read-only roles, HTTP request limits and child cleanup assertions remain unchanged. The12-second unavailable-database variants are separate socket-only checks, not replacements for these database-backed proofs.

### Qualification order

After the dedicated live cases pass, run current registration/migration, the35-member expiry/repair histories and representative row-write/deferred-commit fault tests before the full affected suites. Preserve original business assertions and clocks. Observe fixed-capacity/free/memory/OOM metadata during the selected heavy workloads, retain source identity and sample timing, and report any unsampled fixture/workload explicitly.

Full serial regression must include postgres-store durability/native/registration and affected library tests, operation-controller native tests, scheduler PostgreSQL tests, and the actual owned PostgreSQL controller-service observation tests. Keep existing900s durability and600s native/controller/scheduler gates; select and record the exact existing service test names and their original per-case bounds. Formatting, strict offline/locked all-target/all-feature checks and process/Linux protocol synthetic coverage remain required. Fresh exact-source Linux runtime/artifact qualification is a later full-goal gate, not something this macOS fixture change proves.

Stop and report inadequate capacity, OOM, unexplained timeout/abort, fixture-cleanup uncertainty or history ambiguity; preserve exact evidence. Any policy adjustment needs a separately recorded main decision and new covering proof, not a silent increase in size/timeouts or a reduced workload. B acceptance plus its independent review is required before returning to controller Task8. The full16-stage/callback/service/recovery/single-writer readiness goal is unchanged.

### Main cutoff correction after the first live qualification

The first SQL/explicit-cleanup case passed actual tmpfs/SQL/default-profile observations but failed its first history predicate. Both fixtures were subsequently proven absent by successful exact-ID and exact-name listings. Later bounded projections retained both endpoints with no attributed mount/unmount, but the original failed rows and inspected Created value were not logged; their exact failed predicate cannot be reconstructed from later history.

Main independently tested the installed Docker CLI against a private temporary Unix-socket fake endpoint, without a real daemon or fixture. `--until 0s` transmitted a whole-second integer cutoff even when invoked later within that second. A nanosecond RFC3339 absolute input transmitted its fractional timestamp exactly. This matches upstream duration conversion in [Moby v28 timestamp parsing](https://github.com/moby/moby/blob/v28.0.0/api/types/time/timestamp.go#L23-L25), but the installed-CLI probe supplies the current local behavior evidence. The first observed destroy and failed query completed within the same UTC second, so truncation is consistent with the failure; missing original rows prevent claiming it was the only failed predicate.

Replace only the event query's `--until 0s` value with an explicit UTC RFC3339 nanosecond timestamp captured immediately before that query. The interval remains epoch-to-query-time, not a future wait or enlarged work deadline. Use a private formatter pinned by compiled RED/GREEN. Preserve all existing bracket, ordering, cardinality, ownership, capacity and deadline requirements. Retain inspected Created and the chosen cutoff plus normalized numeric event rows before qualification so subsequent failures identify the actual predicate; never emit arbitrary malformed raw output. Client/daemon clock skew, missing endpoints and incomplete history remain failures/uncertainty. No delay-until-next-second, retry loop, canary or weakened event predicate is authorized.

Preserve the initial failed result, then run one focused corrected SQL/explicit-cleanup invocation. Only a complete pass permits the remaining planned dedicated cases and broader suite. A new unexplained history failure returns to main. This is an audit-query precision repair, not acceptance of the initial failure or production readiness.

### Main observer ruling after the first full durability attempt

The first full durability invocation was terminated by the external observer, not recorded as a completed Rust assertion failure: a fixed cgroup read failed, an immediate exact-ID listing was nonempty, and subsequent exact-ID/name queries confirmed absence. The existing fixture's ordinary cleanup uses exact-owned `rm --force`. Multiple observer reads are not atomic with that cleanup. This supports a possible removal transition, but the original stderr and teardown state were not captured, so the original cause remains unresolved and that invocation remains incomplete.

The ignored external observer may resolve a failed process read with one fixed maximum3-second monotonic exact-ID absence-settlement interval contained within the original Cargo deadline. This is not a retry of the failed capacity/memory read or the workload. Only an empty successful listing proves absence; only exactly the same full ID followed by a newline is an admissible pending response. Foreign/multiple/malformed identities, failed queries, output caps, process timeouts, deadline expiry and ownership/storage/capacity/memory predicate failures remain fatal. Each child query is bounded by both remaining budgets, including its existing finite reap allowance. Check Cargo termination before and after queries, with no new query after observed termination. Bounded polling cadence must not busy-spin or extend the original work deadline.

A resolved disappearance is exclusively a measurement gap with unknown removal cause. It never contributes a passing sample, proves a healthy shutdown, or excludes unobserved OOM/ENOSPC. Retain timing, transition receipts, actual samples, final Rust test result and separate post-terminal cleanup results. Dedicated live audit predicates, fixed storage settings, source cleanup and original tests remain unchanged. Synthetic RED/GREEN covering transitions and negative cases plus main inspection are required before a focused live retry, followed by the original full gates only if the focused result passes. No acceptance follows from this ruling alone.

### Reviewer allocation under the app's agent-thread limit

The app refused a new Astra agent with `agent thread limit reached`. For B only, main may use an existing Astra reviewer with no design or implementation participation in this storage deliverable, supplied a new exact-source review brief and the complete final requirements/evidence. This is an independent review by an uninvolved agent, not a newly spawned or empty-context agent. Preserve both specification and quality verdicts, main acceptance and all original tests; never substitute implementer self-review. The qualification plan records this allocation fallback explicitly. No other scope or readiness requirement changes.
